#First we take the user's input
text = input("Hi, type something: ")

#we store it as a string
text = str(text)

#we need to convert all the text to lowercase
text = text.lower()

#finally, we print the output
print(text)