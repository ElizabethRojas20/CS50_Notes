#import random, imports everything
#coin = random.choice(["heads", "tails"]) #indicate [] list

from random import choice #imports only what I want

coin = choice(["heads", "tails"]) #[] identifies a list
print(coin)